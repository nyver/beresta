// Package account implements local account creation, unlock, and lock
// without any network access: it generates the account identity, device
// signing, database, and initial workspace keys, and owns the keybag
// plaintext's private on-disk encoding.
package account
